<main class="idea-details">
  <div class="idea-card">
    <h1 class="idea-title">{{ideia.titulo}}</h1>
    <p class="idea-description">{{ideia.detalhes}}</p>

    <div class="idea-info">
      <p><strong>Categoria:</strong> {{ideia.categoria.nome}}</p>
      <p><strong>Autor:</strong> {{ideia.criador.nome}}</p>
      <p><strong>Votos:</strong> {{ideia.votoCount}}</p>
    </div>

    <div class="vote-section">
      <button class="vote-btn {{#if ideia.userVoted}}voted{{/if}}" data-idea-id="{{ideia.id}}">
        <span class="vote-icon">👍</span>
        <span class="vote-count">{{ideia.votoCount}}</span>
      </button>
    </div>

    <div class="idea-actions">
      {{#if (eq ideia.criador.id user.id)}}
        <a href="/ideia/{{ideia.id}}/edit" class="edit-btn">✏️ Editar</a>
        <a href="#" class="delete-btn" data-idea-id="{{ideia.id}}">🗑️ Excluir</a>
      {{/if}}
      
      <a href="/ideia" class="back-btn">← Voltar</a>
    </div>
  </div>
  <div id="delete-popup" class="popup-overlay hidden">
  <div class="popup-box">
    <h2>Excluir Ideia</h2>
    <p>Tem certeza que deseja excluir esta ideia?</p>

    <div class="popup-actions">
      <button id="cancel-delete" class="btn cancel">Cancelar</button>
      <button id="confirm-delete" class="btn confirm">Excluir</button>
    </div>
  </div>
</div>

</main>

<link rel="stylesheet" href="/views/ideia/show-ideas/show-ideas.css">
<script src="/views/ideia/show-ideas/show-ideas.js" defer></script>
